name: 'badminton_wrapped'
version: '1.0.0'
config-version: 2

profile: 'badminton_wrapped'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  badminton_wrapped:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
